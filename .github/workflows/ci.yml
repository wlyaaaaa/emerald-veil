name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - run: dotnet restore EmeraldVeil.slnx
      - run: dotnet build EmeraldVeil.slnx --configuration Release --no-restore
      - run: dotnet test EmeraldVeil.slnx --configuration Release --no-build
